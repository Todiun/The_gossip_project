<h1>Gossips#index</h1>
<p>Find me in app/views/gossips/index.html.erb</p>
<h1 class="text-center mb-4">Les potins du moment 💬</h1>

<div class="row">
  <% @gossips.each do |gossip| %>
    <div class="col-md-4 mb-4">
      <div class="card h-100 shadow-sm">
        <div class="card-body">
          <h5 class="card-title"><%= gossip.title %></h5>
          <p class="card-text text-muted">Par <%= gossip.user.first_name %></p>
          <%= link_to "Voir plus", gossip_path(gossip), class: "btn btn-primary" %>
        </div>
      </div>
    </div>
  <% end %>
</div> 